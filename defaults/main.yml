---
# If set to '1', GIT will be compiled and installed from source
# If set to '0', GIT will be installed from https://centos7.iuscommunity.org/ius-release.rpm RPM
git_install_from_source: '0'

# GIT version
git_install_from_source_version: '2.9.5'